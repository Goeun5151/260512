# Copy to .env.local and fill in.
# Kakao Book Search REST API key (https://developers.kakao.com).
# Baked into the client bundle at build time. Without it, book search shows a
# manual-entry notice and the app still works.
NEXT_PUBLIC_KAKAO_REST_KEY=
